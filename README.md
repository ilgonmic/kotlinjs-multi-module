# kotlinjs-multi-module

Since Kotlin 1.3.70 there is npm and webpack support in kotlin-gradle-plugin.

`./gradlew build`

Bundle application:

`./gradlew assemble`

Run application:

`./gradlew :app:run -t`
